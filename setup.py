from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Deploy a best model on AWS which analyse sentiment of a given comment',
    author='mkk',
    license='',
)
